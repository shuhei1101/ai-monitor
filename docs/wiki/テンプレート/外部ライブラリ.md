---
version:
  - number: 1.0.0
    changes: 初版
  - number: 1.1.0
    changes: 不具合一覧とその詳細セクションを追加
  - number: 1.2.0
    changes: 概要を初回の内容のまま保つルールを追加
---

# ai-monitor 規約: 外部ライブラリ

採用した外部ライブラリ 1 つにつき 1 ページの Wiki を作成し、**そのプロジェクトで実際に使う部分だけ** を公式ドキュメントから抽出して整理する。

公式ドキュメントは情報量が多く読みづらいため、ここを見るだけで実装に必要な情報（インストール手順 + 使うメソッドのパラメータ）が揃う状態を目指す。

配置先は `docs/wiki/外部ライブラリ/{ライブラリ名}.md`（例: `docs/wiki/外部ライブラリ/react-image-crop.md`）。
新規作成時は `docs/wiki/外部ライブラリ/README.md`（一覧）に行を追加してリンクを張る。

参照方向は **モジュール構成 → 外部ライブラリ の一方向**。
本ページは依存の最下層として当該ライブラリの情報だけを書き、自プロジェクトのファイル・モジュールには言及しない。

各セクションの書き方を「記述例 + 補足」で定義する。

## セクション一覧

| セクション | サブセクション | 必須or条件 | 担当 | 補足 |
| --- | --- | --- | --- | --- |
| `## 概要` | - | 必須 | architect | - |
| `## 現在のバージョン情報` | - | 必須 | architect | - |
| `## インストール手順` | - | 必須 | architect | - |
| `## API 一覧` | `### {メソッド名 / コンポーネント名}` | 必須（一覧表の各行につき 1 サブセクション） | architect | - |
| `## 不具合一覧` | - | 調査した不具合がある場合のみ | architect | 仕様どおりに使っても期待どおり動かない事象の索引 |
| `### {不具合のタイトル}` | - | 不具合一覧の各行につき 1 セクション | architect | `## 不具合一覧` の配下に置く |

## `## 概要`

### 記述例

```markdown
## 概要

React 用の画像トリミング（クロップ）UI ライブラリ。

矩形・円形・自由比率のトリミング選択と、選択範囲のリアルタイムプレビューを提供する。
出力はトリミング後の Blob / dataURL。
```

### 補足

- ライブラリが何をするものかを 1〜3 行で要約する
- 自プロジェクトでの用途・採用箇所は書かない
- 初回に書いた内容のまま保ち、後から情報を足さない。
  仕様・制限・不具合は `## API 一覧` 以降の各セクションと `## 不具合一覧` に書く（両方に書くと同じ内容を 2 箇所で持つことになる）。
  対象そのものが変わったときだけ既存の記述を書き換える

## `## 現在のバージョン情報`

### 記述例

```markdown
## 現在のバージョン情報

| 項目 | 内容 | 補足 |
| --- | --- | --- |
| バージョン | `v11.0.5` | 2026-07-24 時点最新 |
| ライセンス | MIT | - |
| 公式 URL | https://github.com/DominicTobias/react-image-crop | - |
| 公式ドキュメント | https://github.com/DominicTobias/react-image-crop#readme | - |
```

### 補足

- バージョンは採用時点で固定値を記録し、補足列に **確認日 `YYYY-MM-DD 時点最新`** を書く。
  アップグレードしたらバージョン値と補足日付を同時に更新（メソッド・パラメータも追従して更新する）
- ライセンスは商用利用可否を確認した上で記入（MIT / Apache-2.0 / GPL など）
- 公式 URL は GitHub / npm / 公式サイトのいずれか主要なもの 1 つ
- 公式ドキュメントは API リファレンスがある URL を直接

## `## インストール手順`

### 記述例

````markdown
## インストール手順

```bash
npm install react-image-crop@11.0.5
```

CSS の読み込みも必要:

```ts
import "react-image-crop/dist/ReactCrop.css";
```
````

### 補足

- パッケージマネージャに応じた具体コマンド（npm / pnpm / yarn / uv / pip など）
- バージョンを `@x.y.z` で固定指定
- ピア依存・追加設定（CSS インポート / Tailwind 設定 / 型定義 / 環境変数 など）があれば併記

## `## API 一覧`

このプロジェクトで使うメソッド / コンポーネントを 1 表で俯瞰し、表の下に各メソッドの詳細サブセクション（`### {名前}`）を並べる。

### 記述例

````markdown
## API 一覧

バージョン: `v11.0.5`

| 種別 | 名前 | 用途 | 補足 |
| --- | --- | --- | --- |
| コンポーネント | [`<ReactCrop />`](#reactcrop) | クロップ UI を表示するメインコンポーネント | - |
| 関数 | [`centerCrop()`](#centercrop) | クロップ範囲を画像中央に配置 | - |
| 関数 | [`makeAspectCrop()`](#makeaspectcrop) | アスペクト比を強制したクロップ範囲を生成 | - |

### `ReactCrop`

クロップ UI を表示するメインコンポーネント。

#### パラメータ

| パラメータ | 型 | 必須 | 既定 | 説明 | 補足 |
| --- | --- | --- | --- | --- | --- |
| `crop` | `Crop` | 任意 | `undefined` | 現在のクロップ範囲（controlled 利用時） | 子フィールドは下の行で展開 |
| `crop.unit` | `'%' or 'px'` | 必須 | - | 座標・サイズの単位 | `%`=画像幅比 / `px`=画素絶対値 |
| `crop.x` | `number` | 必須 | - | 左上 X 座標 | - |
| `crop.y` | `number` | 必須 | - | 左上 Y 座標 | - |
| `crop.width` | `number` | 必須 | - | 幅 | - |
| `crop.height` | `number` | 必須 | - | 高さ | - |
| `onChange` | `(crop: PixelCrop, percentCrop: Crop) => void` | 必須 | - | クロップ範囲変更時のコールバック | - |
| `onComplete` | `(crop: PixelCrop) => void` | 任意 | `undefined` | ドラッグ終了時の確定コールバック | プレビュー画像生成用 |
| `aspect` | `number` | 任意 | `undefined` | アスペクト比固定 | `1`=正方形 / `16/9`=横長 / 未指定=自由比率 |
| `circularCrop` | `boolean` | 任意 | `false` | 円形クロップ表示 | 出力データ自体は矩形 |
| `keepSelection` | `boolean` | 任意 | `false` | クリック時に選択範囲を保持 | - |
| `disabled` | `boolean` | 任意 | `false` | クロップ操作を無効化 | - |
| `minWidth` | `number` | 任意 | `0` | 最小幅 (px) | - |
| `maxWidth` | `number` | 任意 | `undefined` | 最大幅 (px) | - |

パラメータ例:

```tsx
<ReactCrop crop={{ unit: "%", x: 25, y: 25, width: 50, height: 50 }} onChange={(c) => setCrop(c)} aspect={1}>
  <img src={src} alt="" />
</ReactCrop>
```

### `centerCrop()`

クロップ範囲を画像中央に配置する。

#### パラメータ

| パラメータ | 型 | 必須 | 既定 | 説明 | 補足 |
| --- | --- | --- | --- | --- | --- |
| `crop` | `Crop` | 必須 | - | 中央配置したいクロップ範囲 | 子フィールドは下の行で展開 |
| `crop.unit` | `'%' or 'px'` | 必須 | - | 座標・サイズの単位 | `%`=画像幅比 / `px`=画素絶対値 |
| `crop.x` | `number` | 必須 | - | 左上 X 座標 | - |
| `crop.y` | `number` | 必須 | - | 左上 Y 座標 | - |
| `crop.width` | `number` | 必須 | - | 幅 | - |
| `crop.height` | `number` | 必須 | - | 高さ | - |
| `mediaWidth` | `number` | 必須 | - | 画像の幅 (px) | - |
| `mediaHeight` | `number` | 必須 | - | 画像の高さ (px) | - |

パラメータ例:

```tsx
import { centerCrop } from "react-image-crop";

const initial = centerCrop({ unit: "%", x: 0, y: 0, width: 50, height: 50 }, 1280, 720);
```

#### 戻り値

| フィールド | 型 | 説明 | 補足 |
| --- | --- | --- | --- |
| `unit` | `'%' or 'px'` | 座標・サイズの単位 | 入力の `crop.unit` を引き継ぐ |
| `x` | `number` | 中央配置後の左上 X 座標 | - |
| `y` | `number` | 中央配置後の左上 Y 座標 | - |
| `width` | `number` | 幅 | - |
| `height` | `number` | 高さ | - |

戻り値例:

```tsx
{ unit: "%", x: 25, y: 25, width: 50, height: 50 }
```

### `makeAspectCrop()`

アスペクト比を強制したクロップ範囲を生成する。

#### パラメータ

| パラメータ | 型 | 必須 | 既定 | 説明 | 補足 |
| --- | --- | --- | --- | --- | --- |
| `crop` | `Partial<Crop>` | 必須 | - | ベースのクロップ範囲 | 子フィールドは下の行で展開 |
| `crop.unit` | `'%' or 'px'` | 必須 | - | 座標・サイズの単位 | `%`=画像幅比 / `px`=画素絶対値 |
| `crop.x` | `number` | 任意 | `0` | 左上 X 座標 | - |
| `crop.y` | `number` | 任意 | `0` | 左上 Y 座標 | - |
| `crop.width` | `number` | 任意 | `undefined` | 幅 | width / height のどちらか必須 |
| `crop.height` | `number` | 任意 | `undefined` | 高さ | width / height のどちらか必須 |
| `aspect` | `number` | 必須 | - | アスペクト比 | `width/height` で計算 |
| `mediaWidth` | `number` | 必須 | - | 画像の幅 (px) | - |
| `mediaHeight` | `number` | 必須 | - | 画像の高さ (px) | - |

パラメータ例:

```tsx
import { makeAspectCrop } from "react-image-crop";

const c = makeAspectCrop({ unit: "%", width: 50 }, 16 / 9, 1280, 720);
```

#### 戻り値

| フィールド | 型 | 説明 | 補足 |
| --- | --- | --- | --- |
| `unit` | `'%' or 'px'` | 座標・サイズの単位 | - |
| `x` | `number` | 左上 X 座標 | - |
| `y` | `number` | 左上 Y 座標 | - |
| `width` | `number` | 幅 | - |
| `height` | `number` | アスペクト比から計算された高さ | `width / aspect` |

戻り値例:

```tsx
{ unit: "%", x: 0, y: 0, width: 50, height: 28.125 }
```
````

### 補足

**一覧表の種別列:**
- `コンポーネント` / `関数` / `フック` / `クラス` / `定数` / `コマンド`（CLI ツールの場合）など

**一覧表の名前列:**
- 詳細サブセクションへのリンクで書く（`[\`名前\`](#{アンカー})`。アンカー = 見出しを小文字化 + 空白を `-` に置換 + 記号除去）

**一覧表の用途列:**
- 1 行で要約。
  詳細パラメータは下の `### {名前}` サブセクション側で書く

**各メソッドサブセクション:**
- 一覧表の各行につき 1 つ作成。
  見出しは `### \`{メソッド名}\``（引数はパラメータ表側で書く）
- 構成: 1〜2 行の説明 → `#### パラメータ`（表 + `パラメータ例:`）→ `#### 戻り値`（表 + `戻り値例:`）
- パラメータ表カラム: `パラメータ` / `型` / `必須` / `既定` / `説明` / `補足` の 6 列固定
- 戻り値表カラム: 単純な値は `型` / `説明` / `補足` の 3 列。
  オブジェクト / JSON を返す場合は `フィールド / 型 / 説明 / 補足` の 4 列で 1 フィールド = 1 行に展開する（パス表記はパラメータ表の入れ子ルールと同じ。返らない / `null` があり得るフィールドは `フィールド?` と表記し、条件を補足列に書く）
- 戻り値が自明なコンポーネントなどは `#### 戻り値` を省略可
- `パラメータ例:` は import 文込みの最小動作版（5〜15 行程度）・`戻り値例:` は返り値の実形（JSON / オブジェクトリテラル）

**パラメータの網羅性:**
- 「現在使っているパラメータ」だけでなく **主要パラメータも全部** 載せる
- 理由: 採用後にチューニングで別パラメータを足す可能性が高く、その都度公式ドキュメントを引き直すのを避けたい
- 採用判断時に「他にどんなパラメータがあるか」が分かる状態にしておく
- 公式ドキュメント全列挙ではなく **主要 = 通常使われる範囲** に絞る（明らかに使わない upstream-only / deprecated は除外）

**「既定」列の書き方:**
- 任意パラメータの省略時デフォルト値を書く（例: `false` / `0` / `undefined` / `'auto'`）
- 必須パラメータは `-` 固定
- 公式が明示してない場合は `undefined` または `-`

**enum パラメータ（値で動作が変わるもの）の書き方:**
- 型列: `'a' or 'b' or 'c'` で取りうる値を列挙
- 補足列: 各値の意味を `a=～ / b=～ / c=～` 形式で書く
- 値が多い（5 個以上など）ときのみ別表 / 別サブセクションに切り出してよい

**データクラス型パラメータ（オブジェクト 1 つを受け取るもの）の書き方:**
- ライブラリは型エイリアスやデータクラス（`Crop` / `Options` など）でパラメータをまとめて渡すケースが多い
- **行を追加して展開する**（API 側の入れ子ルールと同じ）
  - 親行: 型を `Crop` で書き、補足列に「子フィールドは下の行で展開」
  - 子行: `crop.unit` / `crop.x` のように `parent.child` 形式で各フィールドを 1 行ずつ
- **展開は 1 段目まで**（読みやすさ優先）
  - 子フィールドの型がさらに別クラスの場合、孫フィールドは展開しない
  - 孫の構造は子行の補足列に型定義 `Sub = { ... }` で書く
- フィールドが多い（10 個以上） / 同じ型を複数メソッドで使う場合は、別サブセクション `### {型名} 型` に切り出して各メソッドからは「`Crop` 型は下の `### Crop 型` 参照」で済ます

**入れ子（オブジェクト / 配列の中身）の書き方:**
- パラメータ列の表記:
  - オブジェクトの子: `parent.child`
  - 配列要素の子: `parent[].child`
- 親行の補足列に「子フィールドは下の行で展開」と書く

**補足:**
- **メソッド単位では** このプロジェクトで実際に使うものだけ抜粋
- 採用メソッドのパラメータは網羅的に書く（上記「パラメータの網羅性」参照）
- 表の上に **採用しているバージョン** を必ず明記。
  アップグレード時の見直しトリガーになる
- 表とサブセクションの順序を揃える（使用頻度の高い順 or 依存順）

## `## 不具合一覧`

仕様どおりに使っても期待どおり動かない事象の索引。
調べた結果こちらの使い方が誤りだった場合は書かない（正しい使い方を該当該当 API のセクションに反映する）。

### 記述例

```markdown
## 不具合一覧

| タイトル | 概要 | 発生日 |
| --- | --- | --- |
| [onCrop が初回マウント時に発火しない](#oncrop-が初回マウント時に発火しない) | 初期値を渡しても最初の 1 回だけコールバックが呼ばれない | 2026/08/02 |
```

### 補足

**タイトル列:**
- 詳細セクションへのリンクで書く（アンカー = 見出しを小文字化 + 空白を `-` に置換 + 記号除去）

**概要列:**
- 1 行で事象を要約する

**発生日列:**
- 事象を確認した日を `YYYY/MM/DD` 形式で書く。
  外部サービスの挙動は変わるため、いつ時点の情報かを残す

**補足:**
- 新規に調べた不具合は **必ずこの索引にも 1 行追加**（手動更新）
- 解消が確認できた不具合は行と詳細セクションごと消す（残すと古い回避策が使われ続ける）

## `### {不具合のタイトル}`

不具合ごとの詳細。
`## 不具合一覧` の各行につき 1 つの H3 を作成する。

### 記述例

````markdown
### onCrop が初回マウント時に発火しない

> 発生日: 2026/08/02

**概要:**

`crop` に初期値を渡してマウントしても、`onCrop` が最初の 1 回だけ呼ばれない。
ユーザーが 1 度ドラッグすると以降は毎回発火する。

**条件:**

- v11.0.0 以降
- `crop` を制御コンポーネントとして親から渡している

**対処法:**

| 方法 | 結果 | 推奨 |
| --- | --- | --- |
| マウント直後に親側で初期値を 1 度流す | 初回から値が揃う。実装は数行で済む | ○ |
| 非制御コンポーネントとして使う | 発火はするが、親から切り抜き範囲を制御できなくなる | - |
| v10 系に固定する | 発火するが、v11 の性能改善を受け取れない | - |
````

### 補足

**`> 発生日:`:**
- 見出しの直後に引用行で置く。
  事象を確認した日を `YYYY/MM/DD` 形式で書く（ライブラリの挙動は更新で変わるため、いつ時点の話かを明示する）
- 後から再確認して同じ事象が続いていたら日付を更新する

**`**概要:**`:**
- 何が起きるかを事実だけで書く（原因の推測は条件・対処法に寄せる）

**`**条件:**`:**
- 再現する状況を箇条書きで挙げる。
  常に再現するのか、特定の環境だけかが分かるように書く

**`**対処法:**` の表:**

**方法列:**
- 取り得る回避策を 1 行 = 1 案で書く

**結果列:**
- その方法を取ると何が得られ、本来やりたかったことから何がずれるかを書く。
  副作用がないものは「表示される」など得られる状態だけでよい

**推奨列:**
- 最初に試すべき 1 案にだけ `○` を付け、他は `-` にする

**補足:**
- 回避策が無い場合も表は置き、「方法」に `回避策なし`・「結果」に困る内容を書く
