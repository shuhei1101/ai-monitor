# 初期処理

毎ターン（実装・レビュー対応・実行のどの呼び出しでも）最初に実行する。

## 手順

### epic PR の最新状態の取得

MCP `get_issue_or_pr` を呼ぶ:
- `number`: $pr_number
- `is_pr`: true

続けて MCP `list_comments` を呼ぶ:
- `number`: $pr_number
- `is_pr`: true
- `addressee`: `complex-scenario-tester`
- `include_resolved`: false

### フェーズの決定

フェーズ索引のフェーズ一覧を上から順に見て、起動条件に最初にマッチしたフェーズの手順を実行する。
前ターンの記憶ではなく取得結果を判定材料にする（本文・ラベル・コメントはターン間に complex-scenario-writer が変更している可能性がある）。
