# シナリオ修正

story-conductor のエスカレーション対応で決まった方針に沿って単一ユースケースシナリオを修正し、親 story Issue へ完了報告する。

## 手順

### 修正指示の確認

修正指示コメントから、決定した方針とシナリオのどこをどう直すかを読む。

### シナリオファイルの修正

story ブランチの worktree（`.claude/worktrees/{ブランチ名の / を - に置換}`）で対象の単一ユースケースシナリオを修正する。
索引の記述が変わる場合は `docs/wiki/設計図/シナリオ/README.md` も更新する。

修正したファイルを commit + push する。

### 修正指示への返信と Resolve

MCP `reply_comment` を呼ぶ:
- `comment_node_id`: story-conductor の修正指示コメントの node_id
- `sender`: `single-scenario-writer`
- `receiver`: `story-conductor`
- `body`: 修正内容（修正したシナリオページ名 + 積んだ commit の表。commit の書式は共通ルール『コミット報告』）

続けて MCP `resolve_comments` を呼ぶ:
- `node_ids`: 修正指示コメントの `node_id`

### ラベル除去

MCP `transition_phase` を呼ぶ:
- `number`: $pr_number
- `is_pr`: true
- `remove_labels_`:
  - `$AI_MONITOR_LABEL_CONFIRM_SINGLE_SCENARIO_WRITER` の値
- `add_labels_`: なし

### 親 story Issue への完了報告

$pr_number の PR 本文の `## 紐づく Issue` から親 story Issue 番号を得る。

MCP `comment` を呼ぶ:
- `number`: 親 story Issue 番号
- `is_pr`: false
- `sender`: `single-scenario-writer`
- `receiver`: `story-conductor`
- `body`: エスカレーション対応で指示されたシナリオ修正の完了報告（修正したシナリオページ名 + 反映した方針 + 積んだ commit の表）

続けて MCP `add_labels` を呼ぶ:
- `number`: 親 story Issue 番号
- `is_pr`: false
- `labels`:
  - `$AI_MONITOR_LABEL_CONFIRM_STORY_CONDUCTOR` の値

### 作業完了報告

MCP `report_completion` を呼ぶ:
- `agent_name`: `single-scenario-writer`
- `number`: $pr_number
