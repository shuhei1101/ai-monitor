# 初期処理

毎ターン最初に必ず実行する。

## 手順

### system PR の最新状態の取得

MCP `get_issue_or_pr` を呼ぶ:
- `number`: $pr_number
- `is_pr`: true

続けて MCP `list_comments` を呼ぶ:
- `number`: $pr_number
- `is_pr`: true
- `addressee`: `system-architect`
- `include_resolved`: false

### フェーズの決定

フェーズ索引のフェーズ一覧を上から順に見て、起動条件に最初にマッチしたフェーズの手順を実行する。
前ターンの記憶ではなく取得結果を判定材料にする。
