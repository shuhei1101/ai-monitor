# 土台生成（初回）

確定した構成要件をもとに、以降の全エージェントが参照する土台（アーキテクチャ図・`docs/wiki/` 骨格・`docs/rules.yaml`・`README.md`）を system ブランチ上に生成し、GitHub ラベルを一括作成して待機に入る。

## 手順

### 構成要件の確認

system PR 本文の `## 紐づく Issue` から親 system Issue 番号を取り出す。

MCP `get_issue_or_pr` を呼ぶ:
- `number`: 取り出した system Issue の番号
- `is_pr`: false
- `comments`: false

`## 構成要件` の 4 カテゴリ（リポジトリ / サブシステム / 言語 / 外部システム）と `## エピック一覧` を読み、生成する土台の範囲を確定する。
依頼コメントに追加の指定がある場合はそれも読む。

### 作業ディレクトリの準備

system ブランチの worktree（`.claude/worktrees/{ブランチ名の / を - に置換}`。ブランチ名は system PR の `head_ref`）へ移動し、`git pull --ff-only` でリモートの最新を取り込む。

### docs/wiki 骨格の生成

`規約/Wiki管理.md` のトップ構造に沿って `docs/wiki/` のディレクトリを作り、全ディレクトリに目次だけの `README.md` を併置する（書式はテンプレート「README書式」）。

以下は骨格として空の状態で置く。

| 生成物 | 初期状態 |
| --- | --- |
| `docs/rules.yaml` | 空の索引（`rules: []`） |
| `テスト/テスト実行方法.md` | 見出しのみで本文は `未確定` |
| `外部API/README.md` | 空の索引 |
| `外部ライブラリ/README.md` | 空の索引 |

### アーキテクチャ図の作成

`設計図/アーキテクチャ図.md` を構成要件どおりに作成する（`リバースエンジニアリング` ラベルがある場合は master に入っている現状版を上書きする）。

| セクション | 記入内容 |
| --- | --- |
| 全体図 | リポジトリ構成とサブシステムの関係 |
| サブシステム一覧 | サブシステム名・`scope` 列・担当範囲 |
| 外部システム連携 | 連携先・詳細・対応疎通テスト（銘柄未定は `{種別}（銘柄未定）` とし、詳細列と対応疎通テスト列を `-`） |

### README.md の作成

リポジトリルートの `README.md` を、プロジェクトの概要・構成・セットアップ手順の骨格として作成する。

### GitHub ラベルの一括作成

`python -m ai_monitor.setup_labels --repo {owner/name}` を実行し、`constants.env` の全ラベルを対象リポジトリに作成する。

### commit push とタスク一覧のチェック

生成物を commit push する。

system PR 本文の `## タスク一覧` の生成タスクを `[x]` にする。

MCP `update_body` を呼ぶ:
- `number`: $pr_number
- `is_pr`: true
- `body`: 更新後本文

### インラインコメントの投稿

commit した内容を読み返し、共通ルール『インラインコメント』に沿って該当行へ紐づけて投稿する。

- ユーザーに決めてほしい判断がある場合、確認事項を `receiver` にユーザーログイン名を指定して投稿する
- 成果物だけでは意図が読み取れない箇所（選択の理由・見送った案・前提と制約・影響範囲・既知の限界）は、補足事項を `receiver` なしで投稿する

### 完了報告コメントの投稿

MCP `comment` を呼ぶ:
- `number`: $pr_number
- `is_pr`: true
- `sender`: `system-architect`
- `receiver`: ユーザーログイン名（`gh api user --jq '.login'` で取得）
- `format`:
  - `type`: `commits`
  - `body`: 土台生成の完了報告（生成したページ一覧 + 作成したラベルの件数 + 承認・修正方法の案内）
  - `entries`: 積んだ commit の `commit` と `summary` の組

### 議論中 付与 + 待機

MCP `add_labels` を呼ぶ:
- `number`: $pr_number
- `is_pr`: true
- `labels`:
  - `$AI_MONITOR_LABEL_IN_DISCUSSION` の値

続けて MCP `set_assignee` を呼ぶ:
- `number`: $pr_number
- `is_pr`: true

### 作業完了報告

MCP `report_completion` を呼ぶ:
- `agent_name`: `system-architect`
- `number`: $pr_number
