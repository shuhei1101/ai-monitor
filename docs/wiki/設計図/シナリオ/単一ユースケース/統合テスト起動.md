---
template_version: 2.1.0
---

# 統合テスト起動

story-conductor / epic-conductor（復帰呼び出し）が、配下 conductor からの完了報告（全子完了）を受けて統合テスト一式を scenario-writer に委任し、writer からの失敗報告を受けて該当 subsystem Issue にバグを差し戻す単一ユースケース。

対応エージェント: `story-conductor`（単一 UC テスト）/ `epic-conductor`（複合 UC テスト）

- 対応テストファイル: `tests/e2e/単一ユースケース/test_統合テスト起動.py`（読み替え先の epic レベルも同ファイル内で実行する）

図は story レベルで代表する。
epic レベルは以下を読み替えて同型。

| 図の表記 | epic レベルでの読み替え |
| --- | --- |
| story-conductor | epic-conductor |
| single-scenario-writer | complex-scenario-writer |
| story Issue / story PR | epic Issue / epic PR |
| subsystem-conductor / 全子 subsystem | story-conductor / 全子 story |

## 正常シナリオ（統合テストの委任）

### セットアップ

| セットアップ | 説明 | 補足 |
| --- | --- | --- |
| Mock | なし（実環境で実行） | - |
| story Issue | `確認:story-conductor` 付与済み + subsystem-conductor の完了報告コメント（最後の subsystem 完了・自分宛・未解決）あり | 全子 subsystem Issue が closed |
| assignee | 未設定 | エージェント起動条件 |

### フロー

```mermaid
sequenceDiagram
  participant GH as GitHub
  participant ORC as モニター
  participant MON as story-conductor

  Note over MON: 既存セッションを継続利用
  ORC-->>GH: polling（確認ラベル + assignee なし を検知）
  ORC->>MON: 既存セッションへ送信（統合テスト起動）
  activate MON
  MON->>GH: 全子 subsystem の close を確認
  MON->>GH: subsystem-conductor の<br>完了報告コメントを Resolve
  MON->>GH: story PR に<br>確認:single-scenario-writer 付与<br>（統合テスト一式の委任）
  MON->>GH: story Issue の 確認:story-conductor 除去
  deactivate MON
  Note over MON: セッションは epic Issue close まで常駐
```

### 期待値

- subsystem-conductor の完了報告コメントが Resolve 済み
- story PR に `確認:single-scenario-writer` が付与されている
- `確認:story-conductor` が除去されている（`議論中` は付与されず assignee は未設定のまま）

## 正常シナリオ（未完了の子が残っている場合）

### セットアップ

| セットアップ | 説明 | 補足 |
| --- | --- | --- |
| Mock | なし（実環境で実行） | - |
| story Issue | `確認:story-conductor` 付与済み + subsystem-conductor の完了報告コメント（自分宛・未解決）あり | - |
| 子 subsystem Issue | open の subsystem が残っている | 完了報告した subsystem 以外が進行中 |
| assignee | 未設定 | エージェント起動条件 |

### フロー

```mermaid
sequenceDiagram
  participant GH as GitHub
  participant ORC as モニター
  participant MON as story-conductor

  Note over MON: 既存セッションを継続利用
  ORC-->>GH: polling（確認ラベル + assignee なし を検知）
  ORC->>MON: 既存セッションへ送信（統合テスト起動）
  activate MON
  MON->>GH: 子 subsystem に open が<br>残っていることを確認
  MON->>GH: subsystem-conductor の<br>完了報告コメントを Resolve
  MON->>GH: open の各 subsystem Issue に<br>状況確認コメント投稿<br>（@subsystem-conductor 宛・進捗と<br>ブロッカーの確認）+<br>確認:subsystem-conductor 付与
  MON->>GH: story Issue の 確認:story-conductor 除去<br>（残りの子の完了報告で再復帰する）
  deactivate MON
  Note over MON: セッションは epic Issue close まで常駐
```

### 期待値

- subsystem-conductor の完了報告コメントが Resolve 済み
- open の subsystem Issue に状況確認コメント（@subsystem-conductor 宛・未解決）+ `確認:subsystem-conductor` が投稿・付与されている
- `確認:story-conductor` が除去されている（統合テストの委任は発生しない）

## 正常シナリオ（状況確認への応答）

### セットアップ

| セットアップ | 説明 | 補足 |
| --- | --- | --- |
| Mock | なし（実環境で実行） | - |
| subsystem Issue | `確認:subsystem-conductor` 付与済み + story-conductor の状況確認コメント（自分宛・未解決）あり | 未完了の子が残っている場合の続き |
| 進行状況 | subsystem は architect へ委任済みで作業中 | 応答する内容がある状態 |
| 索引の他フェーズ | どの起動条件にもマッチしない | 汎用の応答フェーズが選ばれる条件 |
| assignee | 未設定 | エージェント起動条件 |

### フロー

```mermaid
sequenceDiagram
  participant GH as GitHub
  participant ORC as モニター
  participant MON as subsystem-conductor

  Note over MON: 既存セッションを継続利用
  ORC-->>GH: polling（確認ラベル + assignee なし を検知）
  ORC->>MON: 既存セッションへ送信
  activate MON
  MON->>MON: 索引の他フェーズに該当せず<br>未解決の自分宛コメントがあると判定
  MON-->>GH: 自 Issue / PR の状態から<br>進捗とブロッカーを確認
  MON->>GH: 状況確認コメントに現状を返信追記して Resolve
  MON->>GH: subsystem Issue の 確認:subsystem-conductor 除去
  deactivate MON
```

### 期待値

- 状況確認コメントのスレッドに現状（進捗とブロッカー）が返信追記され、Resolve 済み
- subsystem Issue から `確認:subsystem-conductor` が除去されている（問い合わせに答えて手番を返す）
- 新しい Draft PR が作成されていない（作業済みの subsystem に対して要件確定をやり直していない）
- 本文・タスク一覧が変更されていない
- 進行中の子（architect の担当）のラベル・assignee が変わっていない

## 正常シナリオ（バグ差し戻し）

### セットアップ

| セットアップ | 説明 | 補足 |
| --- | --- | --- |
| Mock | なし（実環境で実行） | - |
| story Issue | `確認:story-conductor` 付与済み + single-scenario-writer の失敗報告コメント（実装側の問題・fail 内容・自分宛・未解決）あり | - |
| 該当 subsystem Issue | closed（PR は story へ merged 済み） | 差し戻し先 |
| ユーザー役 | 対応方針の承認（`議論中` 除去）を pytest が実施 | 実装修正 = 設計書の修正を伴うため差し戻し前に確認 |
| assignee | 未設定 | エージェント起動条件 |

### フロー

```mermaid
sequenceDiagram
  actor U as ユーザー
  participant GH as GitHub
  participant ORC as モニター
  participant MON as story-conductor

  Note over MON: 既存セッションを継続利用
  ORC-->>GH: polling（確認ラベル + assignee なし を検知）
  ORC->>MON: 既存セッションへ送信（バグ差し戻し）
  activate MON
  MON->>GH: story Issue に対応方針案コメント<br>（修正対象 subsystem + 修正内容の要約 +<br>対応要否）+ 議論中 付与 +<br>assignee=ユーザー 設定
  deactivate MON

  loop 応答ループ（方針の調整がある間）
    U->>GH: story Issue に調整コメント +<br>assignee 外し
    ORC-->>GH: polling（ユーザー返信 + assignee なし を検知）
    ORC->>MON: 既存セッションへ送信
    activate MON
    MON->>GH: 方針案を更新 +<br>assignee=ユーザー 再設定
    deactivate MON
  end

  U->>GH: story Issue の 議論中 除去 +<br>assignee 外し（方針の承認）
  ORC-->>GH: polling（議論中 除去 + assignee なし を検知）
  ORC->>MON: 既存セッションへ送信（差し戻し実行）
  activate MON
  MON->>GH: 該当する各 subsystem Issue を reopen +<br>バグ内容コメント（fail 内容 + 修正方針・<br>@subsystem-conductor 宛）+<br>確認:subsystem-conductor 付与
  MON->>GH: 失敗報告コメントに差し戻し結果<br>（対象 subsystem Issue 番号）を返信追記
  MON->>GH: story Issue の自分宛コメント一括 Resolve
  MON->>GH: story Issue の 確認:story-conductor 除去
  deactivate MON
```

### 期待値

- 該当 subsystem Issue が open（reopen 済み）で、`確認:subsystem-conductor` + バグ内容コメント（fail 内容 + 修正方針・@subsystem-conductor 宛・未解決）が付与・投稿されている
- 失敗報告コメントのスレッドに差し戻し結果（対象 subsystem Issue 番号）が返信追記され、Resolve 済み
- story Issue に対応方針案コメントが投稿されている（Resolve 済み）
- `確認:story-conductor` が除去されている（`議論中` はユーザーが除去済み）

## 異常シナリオ

なし
