---
template_version: 2.0.0
---

# 設計差し戻しからの設計修正

worker（tester / implementer）が設計 Wiki どおりに進められない構造問題を検知して指揮役の architect に直接差し戻し、architect が設計 Wiki を修正（ユーザー承認あり）してから元の工程に復帰するまでの複合ユースケース。

**E2E テストの位置付け:** worker 起点の差し戻し → 設計修正 → 工程復帰の循環が dead lock せず前進することの確認。

- 対応テストファイル: `tests/e2e/複合ユースケース/test_設計差し戻しからの設計修正.py`

## 正常シナリオ（テスト作成の設計差し戻し）

### セットアップ

| セットアップ | 説明 | 補足 |
| --- | --- | --- |
| Mock | なし（実環境で実行） | - |
| sandbox リポ状態 | subsystem PR に `確認:tester` 付与済み・設計 Wiki 確定済み | テスト作成開始直前の状態 |
| 矛盾の埋込 | 設計 Wiki のモジュール構成に、テストを書けない構造矛盾を仕込む（一覧のコンテナとディレクトリ構成で import 先が食い違う・単体テスト表が要求する更新履歴 / 楽観ロックを表す設計要素がどこにもない） | 設計の見直しを誘発 |
| ai-monitor 起動 | モニターが polling 中 | - |
| ユーザー役 | 設計 Wiki 修正の承認（`議論中` 除去）を pytest が実施 | - |

### フロー

```mermaid
flowchart TD
  A0([architect]) -->|設計 Wiki 確定後に subsystem PR に<br>確認:tester 付与| UC1

  subgraph FOCUS["検証対象: 差し戻し → 設計修正 → 工程復帰"]
    UC1([テスト作成:異常シナリオ<br>（設計の見直しが必要）]) -->|設計の見直しが必要・確認:architect +<br>差し戻し報告| UC2([SS設計:正常シナリオ<br>（差し戻しからの設計修正）])
    UC2 -->|設計 Wiki 修正 commit・ユーザー承認 →<br>スレッドに再開指示を返信追記 +<br>確認:tester 再付与| UC3([テスト作成:正常シナリオ])
  end

  UC3 -->|テストコード commit + 確認:architect +<br>完了報告| DONE([subsystem PR: 確認:architect +<br>tester の完了報告コメントあり])

  click UC1 "../単一ユースケース/テスト作成.md#異常シナリオ設計の見直しが必要"
  click UC2 "../単一ユースケース/SS設計.md#正常シナリオ差し戻しからの設計修正"
  click UC3 "../単一ユースケース/テスト作成.md#正常シナリオ"
```

### 期待値

- tester の差し戻し報告コメントのスレッドに architect の修正内容（修正 commit の ID）と再開指示が返信追記され、tester の処理を経て Resolve 済み
- 設計 Wiki の修正 commit が subsystem ブランチに積まれている（修正はユーザー承認済み）
- 修正後の設計 Wiki に基づくテストコードとテスト結果表のファイル名行が存在する
- subsystem PR に `確認:architect` + tester の完了報告コメント（未解決）が付与・投稿されている
- 循環経路（差し戻し → 設計修正 → テスト再作成）の全ラベル遷移が完了している（`確認:tester` / `確認:architect` が多重に残っていない）

## 正常シナリオ（実装の設計差し戻し）

### セットアップ

| セットアップ | 説明 | 補足 |
| --- | --- | --- |
| Mock | なし（実環境で実行） | - |
| sandbox リポ状態 | subsystem PR に `確認:implementer` 付与済み・テストレビュー済み（Red 確認済み） | 実装開始直前の状態 |
| 矛盾の埋込 | 設計 Wiki のモジュール構成を「戻り値なし・更新は `store` に反映」で一貫させる（レビュー済みのテストは戻り値のタスクを検証するため、シグネチャを変えないと実装できない） | 設計レベルの判断を誘発。テストコードの修正は不要 |
| ai-monitor 起動 | モニターが polling 中 | - |
| ユーザー役 | 設計 Wiki 修正の承認（`議論中` 除去）を pytest が実施 | - |

### フロー

```mermaid
flowchart TD
  A0([architect]) -->|テストレビュー後に subsystem PR に<br>確認:implementer 付与| UC1

  subgraph FOCUS["検証対象: 差し戻し → 設計修正 → 工程復帰"]
    UC1([実装:異常シナリオ<br>（設計レベルの判断が必要）]) -->|設計レベルの判断が必要・確認:architect +<br>差し戻し報告| UC2([SS設計:正常シナリオ<br>（差し戻しからの設計修正）])
    UC2 -->|設計 Wiki 修正 commit・ユーザー承認 →<br>スレッドに再開指示を返信追記 +<br>確認:implementer 再付与| UC3([実装:正常シナリオ])
  end

  UC3 -->|実装 commit + 確認:architect + 完了報告| UC4([実装レビュー:正常シナリオ])
  UC4 -->|指摘なし・Green の実測確認 + Ready 化 +<br>タスク一覧のテスト実行チェック +<br>確認:subsystem-conductor + 一式完了報告| DONE([subsystem PR: 確認:subsystem-conductor +<br>一式完了報告コメントあり])

  click UC1 "../単一ユースケース/実装.md#異常シナリオ設計レベルの判断が必要"
  click UC2 "../単一ユースケース/SS設計.md#正常シナリオ差し戻しからの設計修正"
  click UC3 "../単一ユースケース/実装.md#正常シナリオ"
  click UC4 "../単一ユースケース/実装レビュー.md#正常シナリオ"
```

### 期待値

- implementer の差し戻し報告コメントのスレッドに architect の修正内容（修正 commit の ID）と再開指示が返信追記され、implementer の処理を経て Resolve 済み
- 設計 Wiki の修正 commit が subsystem ブランチに積まれている（修正はユーザー承認済み）
- 修正後の設計 Wiki に基づく実装 commit が積まれ、テスト結果表が全 ✅・PR が Ready 状態
- `## タスク一覧` の全行がチェック済み
- subsystem PR に `確認:subsystem-conductor` + 一式完了報告コメント（未解決）が付与・投稿されている
- 循環経路（差し戻し → 設計修正 → 実装再開）の全ラベル遷移が完了している（`確認:implementer` / `確認:architect` が多重に残っていない）

## 異常シナリオ

なし
