# ai-monitor ワークフロー設計

## 本ファイルの扱い

本ファイルは全セッションのコンテキストに常時載る。
放っておくと決定事項と完了済みタスクが積み上がってコンテキストを汚染し、判断の精度が落ちる。
**最終的に空にすることを目標に、常に減らす方向で保つ。**

| タイミング | やること |
| --- | --- |
| 実装が完了したとき | 該当する記述を削除し、参照先（Wiki / 設計書 / コード）へのリンクだけを残す |
| Wiki へ移行したとき | 本文をここから消し、移行先のパスだけ残す |
| 方針が変わったとき | 古い記述を書き換えるのではなく、まず不要になった部分を消す |

書き足すのは「まだどこにも置き場がない決定」だけにする。
置き場ができたらそちらへ移し、ここからは消す。

エージェント関連の設計は以下が SoT。

| 内容 | 参照先 |
| --- | --- |
| エージェントのワークフロー（工程・正常 / 異常フロー） | `docs/wiki/設計図/シナリオ/`（単一ユースケース / 複合ユースケース） |
| 各エージェントのフェーズ手順 | `docs/wiki/エージェント/{エージェント名}/` |
| レイヤー判定基準 | `docs/wiki/判定フローチャート/レイヤー.md` |
| ラベル値 | `plugins/ai-monitor/constants.env` |

## ドキュメントの SoT

| 対象 | SoT |
| --- | --- |
| Issue 本文 | `テンプレート/イシュー本文/`（エピック / ストーリー / サブシステム） |
| PR 本文 | `テンプレート/PR本文/`（エピック / エピックPoC / ストーリー / サブシステム / ライブラリPoC / 軽微修正） |
| Wiki の管理ルール | `規約/Wiki管理.md` |
| 設計書とテストの対応 | `規約/V字モデル.md` |
| dev-kit の規約 | my-plugins `docs/rules/`（索引は `docs/rules.yaml`） |
| MCP ツール | `設計図/インターフェース定義/バックエンド/`（1 ツール = 1 ファイル）+ `設計図/モジュール構成/MCP/` |

いずれも担当エージェントは `architect`（仕様駆動で SS 確定と同一 PR 内に更新 commit を積む）。

## E2E テストの実行（メモ）

実行コマンド・実行前後の確認手順（反映経路 / jsonl の機械チェック）は `テスト/テスト実行方法.md` の `## E2E テスト` が SoT。

実行後のログ確認は jsonl だけで終わらせず、observability-tools の MCP（`search_logs` / `search_traces`）も必ず見る。
`success=false` で絞ると、テストが pass していても拾えていない不具合が出てくる（MCP に拒否された操作・GitHub API の一時エラーからのエージェント自力リカバリ 等）。
`ai_monitor_agent` / `ai_monitor_number` / `ai_monitor_project` 属性で対象ごとに絞り込める。

## プラグイン更新時のバージョン同期（メモ）

`plugins/` 配下を更新したときは、以下 2 ファイルのバージョンを同じ値に**同時にインクリメント**する。
片方だけ上げてもキャッシュが同期されず、ユーザーの `/plugin` 再インストールや `/reload-plugins` でも新しい実装が反映されない。

- `.claude-plugin/marketplace.json` の `plugins[].version`
- `plugins/ai-monitor/.claude-plugin/plugin.json` の `version`

反映は「push（ユーザー） → `/plugin` でマーケットプレイス更新 → `/reload-plugins` → `~/.claude/plugins/cache/ai-monitor/ai-monitor/{新バージョン}/` の生成確認」の順。

## エージェントのフェーズ作成時の必読資料（メモ）

エージェントのフェーズページを書くとき、以下は必ず全て読んでから着手する（読まずに書くと担当・書式の齟齬が起きる）:

- `docs/wiki/設計図/シナリオ/単一ユースケース/` の該当 UC（そのエージェントの正常系フロー）
- `docs/wiki/設計図/シナリオ/複合ユースケース/` の関連 UC（そのエージェントが登場する複合経路）
- `docs/wiki/テンプレート/PR本文/` / `docs/wiki/テンプレート/イシュー本文/` のうち、そのエージェントが書き込む本文テンプレ（担当列・セクション書式・記述例）

## ドキュメント執筆の禁則（メモ）

- **「〜は書かない」式の禁止注釈をドキュメントに追加しない**。記述例に無い内容はそもそも出力されない。禁止の列挙は情報量ゼロでコンテキストを汚し精度を下げる
- **ユーザーの発言を一言一句ドキュメントに起こさない**。文脈から記載要否を判断する
- 補足に書くのは正の指示（何をどう書くか）だけ

## 今後の TODO（メモ）

### dev-kit（my-plugins `docs/rules/`）の併走保守（方針・2026-07-20）

- ai-monitor の設計議論で決めた内容が dev-kit 規約（言語 / フレームワーク横断）と食い違ったら、**dev-kit 側も修正対象**として随時更新する（古い記述が残っているため、こちらを整えながら進める）
- **将来タスク（ai-monitor 稼働後）**: エージェントが dev-kit 規約を直接修正するのは現行（稼働前）の暫定運用。稼働後は、機能改修の議論中にユーザーの決定が dev-kit 規約と矛盾すると発覚した場合、エージェントは規約を直接修正せず **my-plugins リポジトリに修正内容の Issue を起票 + `確認:intake-issue-triager` を付与**して、my-plugins 側のワークフローで回すルートにする（前提: dev-kit 側の設計書整備 + my-plugins をモニターの監視対象プロジェクトに登録。ai-monitor の整備完了後に着手）
- 影響範囲の当たりは my-plugins `docs/rules.yaml`（全ルール索引・inject-rules が `paths` の glob でマッチしたルールを自動注入する）を参照



### データフロー（DFD）の レベル別担当（要検討）

- Level 0（Context）= **epic 段階** で作成 / 更新（全体俯瞰）
- Level 1 = **story 段階**（1 UC / 1 データ の主要プロセス）
- Level 2 以降 = **subsystem 段階**（実装単位の詳細）
- 実装粒度と対応する構造
- どのエージェントがどのレベルを触るかは、実装時に精査

### エージェント連絡網の起動プロンプトへの注入（実装したい）

- `プロジェクト管理/エージェント組織図.md`（連絡網の SoT）の自分の行を各エージェントの起動プロンプトに載せ、エージェントが「自分が連絡できる相手」だけを認知した状態で起動するようにする
- 起動プロンプトは タスクのやり方 + 関連 Issue/PR の情報 + 連絡先 を持ち、不具合・相談時に連絡先のエージェントへ `確認:{宛先}` + @宛先コメントで連絡できる
- 連絡網にない相手への送信は MCP 側でバリデーションする（MCP 権限境界と合わせて設計）



### 本 CLAUDE.md の全内容を Wiki に移行（方針確定済み）

最終的に本ファイルは空にする。移行が終わっていないセクションと移行先:

| セクション | 移行先 | 状態 |
| --- | --- | --- |
| ラベル一覧 | `constants.env`（値の SoT）+ `プロジェクト管理/ラベル定義一覧.md`（運用ルール） | env 済 / Wiki 未 |
| 全体俯瞰 | `設計図/アーキテクチャ図.md` に 1 枚（フローは複合シナリオ 4 本で代替済み） | 未 |
| エージェント詳細 24 個 | 各エージェントのフェーズページ + `Claudeハーネス/エージェント一覧.md` | フェーズ 24/24 済 / 一覧未 |
| 各エージェント質問案 | 各フェーズページ内に埋め込み（まとめページは作らない） | 一部済 |
| 今後の TODO | GitHub Issues へ（まとめページは作らない） | 未 |

運用ルール（tmux モデル / assignee / 議論中 / 呼び出しモデル / 並列実行）は各フェーズページ・`Claudeハーネス/共通ルール/`・`規約/コメント.md`・`設計図/モジュール構成/モニター/` に実装済みのため、まとめページは作らずに削除した。

**`Claudeハーネス/エージェント一覧.md` の要件**:
- 全 24 エージェントの一覧表: 監視条件（ラベル + assignee）/ 対応 skill / 遷移先ラベル / **使用 Claude モデル**（sonnet / opus 等）
- モデル指定の実体は `settings.yaml` の `agents[].model`。一覧はその索引（二重管理にしない、値の SoT は設定側）

**新規作成する Wiki ページ**: Claudeハーネス/エージェント一覧.md / プロジェクト管理/ラベル定義一覧.md / 設計図/アーキテクチャ図.md

動的注入対応表 / フック一覧 / プラグイン構成は作らない。
それぞれ `hooks.json` / inject-rules の `rules.yaml` / `marketplace.json` が SoT を持っており、Wiki に置くと二重管理になる。

### モニターの state store は YAML（決定済み）

- SQLite ではなく **YAML ファイル**（`data/state.yaml`）で持つ
- 理由: ワークフロー状態の SoT は GitHub（ラベル / assignee）、実行中セッションの SoT は tmux 自体（`tmux ls`）。永続化が必要なのはセッション台帳（heartbeat の last_seen・監視面の番号一覧）程度の数十行のみ（conductor の PR → Issue の逆引きは毎回 PR 本文の `## 紐づく Issue` から解決・worker の PoC PR は台帳の監視面で解決）
- 書き込みは tmp ファイル → rename のアトミック書き
- SQLite への移行が要るほどの規模になったとき見直す
